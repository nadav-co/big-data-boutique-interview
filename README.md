# big-data-boutique-interview

This is a meeting management app for a single meeting room.
To start with this project open 2 command-lines.
In the first run:

    cd /frontend 
    npm i 
    npm start

In the other run:

    cd /backend
    npm i 
    npm run server:dev
